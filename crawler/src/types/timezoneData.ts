export interface TimezoneDataParse {
    abbreviation?: string,
    name?: string,
    utcOffset?: string
};

export interface TimezoneData {
    abbreviation: string,
    name: string,
    utcOffset: string
};